export type CodeForm = {
  [key: string]: string;
  language: string;
  title: string;
  code: string;
};

export type TypeCodeFormValidationSchema = {
  [key: string]: string;
  language: string;
  title: string;
  code: string;
};
