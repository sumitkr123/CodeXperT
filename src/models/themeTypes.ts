export interface Theme {
  isDark: boolean;
  background: string;
  text: string;
  primary: string;
  secondary: string;
  backIconColor: string;
  iconColor: string;
  headerBarColor: string;
  textInputBorderColor: string;
}
